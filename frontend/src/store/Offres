import { defineStore } from "pinia";
import { computed, ref } from "vue";

export const useOffreStore = defineStore("offreStore", () => {
  const offres = ref([]);

  const allOffres = computed(() => offres.value.length);

  return { offres, allOffres };
});
